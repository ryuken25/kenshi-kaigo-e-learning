import React,{useState} from 'react';
import {Link} from 'react-router-dom';
import {ChevronRight,Check} from 'lucide-react';
function Mascot(){return <div className="mascotImg size-md"><img src="/assets/hellokitty/hk-cute-emoji.png" alt="Hello Kitty"/></div>}
export default function Login(){const [email,setEmail]=useState(''),[state,setState]=useState('idle');const submit=async e=>{e.preventDefault();setState('sending');try{const r=await fetch('/api/auth/magic-link',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email})});const d=await r.json();if(!r.ok)throw Error(d.error);setState('sent')}catch(e){setState(e.message||'error')}};return <main className="page authPage"><Mascot/><h1>Welcome to Kaigo Kitty</h1><p className="muted">Masuk dengan magic link. Tidak perlu password.</p>{state==='sent'?<div className="objective"><Check/><div><b>Link sudah dikirim ✨</b><p>Cek inbox {email}. Link berlaku 20 menit.</p></div></div>:<form className="authForm" onSubmit={submit}><label>Email kamu<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><button className="primary big tap" disabled={state==='sending'}>{state==='sending'?'Mengirim…':'Kirim magic link'} <ChevronRight/></button>{state!=='idle'&&state!=='sending'&&<small className="authError">{state}</small>}</form>}<p className="authNote">Google OAuth menyusul setelah magic-link flow stabil.</p><Link className="back" to="/">‹ Kembali</Link></main>}
